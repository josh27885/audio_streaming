name: audio_streaming
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  
  # Ensure there are exactly 2 spaces before these package names!
  record: ^5.0.4
  web_socket_channel: ^2.4.0 
  permission_handler: ^11.3.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
